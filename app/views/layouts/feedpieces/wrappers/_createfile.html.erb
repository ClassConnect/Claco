<div class="thetitle">
	<a href="<%= @wholink %>"><%= "#{@who.fname} #{@who.lname}" %></a> added <%= @howmany %> file<%= @mult ? 's' : ''%> to <a href="<%= named_binder_route(@where) %>"><%= @where.title %></a> [[[TIMESTAMP]]] ago.
</div>

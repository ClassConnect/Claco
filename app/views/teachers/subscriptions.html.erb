<ul>
<% @subscriptions.each do |sub| %>
<li>
<div><%= image_tag(sub.info.avatar.url) %></div>
<div><%= link_to(sub.full_name, sub.username) %></div>
<div><%= sub.info.bio %></div>
</li>
<% end %>
</ul>
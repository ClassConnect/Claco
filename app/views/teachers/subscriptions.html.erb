<div class="headtitle">
      Subscriptions
      <a href="#" onClick="closefBox()" class="btn btn-mini pull-right">&times;</a>
</div>
<ul>
    <% @subscriptions.each do |sub| %>
    <li>
        <%= image_tag(teacher_thumb_md(sub), {:class => 'thumbster'}) %>
        <% if !signed_in? %>
            <a class="btn btn-mini subbtn pull-right" href="/login">Subscribe</a>
        <% elsif sub.id.to_s == current_teacher.id.to_s %>
        <% elsif !current_teacher.subscribed_to?(sub.id) %>
            <button class="btn btn-mini subbtn pull-right" data-action="subscribe" data-url="<%= sub.username %>/subscribe" type="button">Subscribe</button>
        <% else %>
            <span class="pull-right">✓ Subscribed!</span>
        <% end %>
        <h3><%= link_to(sub.first_last, sub.username) %></h3>
        <hr>
    </li>
    <% end %>
</ul>
